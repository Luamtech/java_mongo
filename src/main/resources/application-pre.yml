# src/main/resources/application-pre.yml  
spring:
  config:
    activate:
      on-profile: pre

logging:
  level:
    root: DEBUG
    com.pakal.cloud: DEBUG
  file:
    name: logs/application.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

---